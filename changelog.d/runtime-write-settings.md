---
category: Added
---

- **Configure MCP writes without restarting.** Standalone MCP tools can inspect and change write permissions and organization scope; automatic approval now delegates all enabled writes, including raw GraphQL, to the AI client's tool-call permission policy.
